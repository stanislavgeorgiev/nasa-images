'use strict';

describe('nasaImagesApp.photo module', function() {

  beforeEach(module('nasaImagesApp.photo'));

  describe('photo controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('PhotoCtrl');
      expect(ctrl).toBeDefined();
    }));

  });
});