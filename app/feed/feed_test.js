'use strict';

describe('nasaImagesApp.feed module', function() {

  beforeEach(module('nasaImagesApp.feed'));

  describe('feed controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ctrl = $controller('FeedCtrl');
      expect(ctrl).toBeDefined();
    }));

  });
});